{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "lerna publish",
    "test": "lerna run test --",
    "test:junit": "yarn test | tap-junit --output test-results -n result.xml"
  },
  "devDependencies": {
    "lerna": "3.21.0",
    "tap-junit": "3.1.2"
  }
}
