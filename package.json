{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "lerna publish",
    "test": "lerna run test --",
    "test:junit": "yarn test | tap-junit --output test-results -n result.xml"
  },
  "devDependencies": {
    "lerna": "3.22.1",
    "tap-junit": "3.1.2"
  }
}
