{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --",
    "test:junit": "yarn test | tap-junit --output test-results -n result.xml"
  },
  "devDependencies": {
    "lerna": "^3.19.0",
    "tap-junit": "^3.1.0"
  }
}
