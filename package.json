{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "lerna publish",
    "test": "pnpm test -r",
    "test:junit": "pnpm test -r | tap-junit --output test-results -n result.xml"
  },
  "devDependencies": {
    "tap-junit": "5.0.2"
  }
}
